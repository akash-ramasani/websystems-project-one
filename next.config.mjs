/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com', 'images.unsplash.com'], // Allow Pexels domain for external images
    },
  };
  
  export default nextConfig;